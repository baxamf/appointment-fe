export enum RoutePaths {
  ADMIN_SERVICES = "/admin/services",
  ADMIN_STAFF = "/admin/staff",

  HOME = "/",
  SERVICES = "/services",
  ABOUT = "/about",
  ARTISTS = "/artists",
  APPOINTMENT = "/appointment",
}
